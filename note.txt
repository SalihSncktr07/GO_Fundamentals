# kod yukarıdan aşağıya okunur

//  tek satır yorum

/*
    birden fazla yorum satırı
*/

# go mod init golesson <- komutu

# birden fazla package eklenirken () arasına yazılı, paketler arasına birşey konulmaz!!

# go dilinde kullanılmayan değişkenler ve paketler hata veriri, sadece kullanılan kodlar yazılır

# else if sadece 2 den fazla olan durumlarda kullanılır, eğer sadece iki durum varsa if ve else kullanılır.

# mainden functiona göderilern sayıda işlem yapılsa bile mainde sayı ilk hali ile kalır func kısmı değişir

# fonksyon isimlerinin başı büyük olmalı

# mainde çağırma metodunun başına go yazılırsa onu çalıştırmadan bir sonraki işleme geçer

# GET okumak için, POST silme, güncelleme ve ekleme işlemleri yapılı

# https://mholt.github.io/json-to-go/ ile json kodunu go koduna çevirir

# Unmarshal json tipindeki yapıyı stringe çevirir

# Marshal string tipindeki yapıyı json a çevirir